# Internals

This folder contains the `Internals` part of the Expert roadmap.

## What We Did
- Created the `internals` folder inside `src/expert`.
- Started this section with `react-native-bridge-internals`.
- Added the second topic: `jsi`.
- Added the third topic: `fabric-renderer`.
- Added the fourth topic: `hermes-engine-tuning`.
- Added this section summary file as the running log for this folder.

## Current Topics
- React Native bridge internals
- JSI
- Fabric renderer
- Hermes engine tuning

## Current Structure
- `react-native-bridge-internals`
- `jsi`
- `fabric-renderer`
- `hermes-engine-tuning`

## Notes
- Expert internals work in this section should reflect how modern React Native actually runs today, including the New Architecture being the default, while still explaining legacy bridge behavior for migration and debugging.

## Update Rule
- This file is the running summary for the `internals` folder.
- Whenever files or examples are added, removed, or changed inside this folder, this `README.md` should be updated to reflect the latest state.
