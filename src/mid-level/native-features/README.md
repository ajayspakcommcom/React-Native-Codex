# Native Features

This folder contains the `Native Features` part of the Mid-Level roadmap.

## What We Did
- Created the `native-features` folder inside `src/mid-level`.
- Started this section one topic at a time.
- Created the first topic folder: `permissions`.
- Created the second topic folder: `camera-media-location`.
- Created the third topic folder: `mmkv`.
- Added this section summary file as the running log for this folder.

## Current Topics
- Permissions
- Camera, Media, Location
- AsyncStorage / MMKV

## Current Structure
- `permissions`
- `camera-media-location`
- `mmkv`

## Notes
- Native features usually need real package installation and native platform setup.
- This section should use actual iOS and Android configuration where needed instead of fake UI-only examples.
- The `mmkv` topic now uses a multi-domain storage architecture with migrations instead of a single flat key-value store.

## Update Rule
- This file is the running summary for the `native-features` folder.
- Whenever files or examples are added, removed, or changed inside this folder, this `README.md` should be updated to reflect the latest state.
