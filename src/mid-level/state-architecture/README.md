# State & Architecture

This folder contains the `State & Architecture` part of the Mid-Level roadmap.

## What We Did
- Created the `state-architecture` folder inside `src/mid-level`.
- Started this section one topic at a time.
- Created the first topic folder: `hooks-in-depth`.
- Created the second topic folder: `context-api`.
- Created the third topic folder: `redux-toolkit-rtk-query`.
- Created the fourth topic folder: `clean-component-structure`.
- Added this section summary file as the running log for this folder.

## Current Topics
- Hooks in depth
- Context API
- Redux Toolkit / RTK Query
- Clean component structure

## Current Structure
- `hooks-in-depth`
- `context-api`
- `redux-toolkit-rtk-query`
- `clean-component-structure`

## Notes
- This section will be implemented incrementally, not all at once.
- The roadmap originally listed `Redux Toolkit / Zustand`.
- For this project, that item is intentionally adjusted to:
  `Redux Toolkit / RTK Query`
- This better fits a more production-oriented Mid-Level learning path.

## Update Rule
- This file is the running summary for the `state-architecture` folder.
- Whenever files or examples are added, removed, or changed inside this folder, this `README.md` should be updated to reflect the latest state.
